package com.fadlurahmanfdev.livefacex.core.enums

/**
 * face detection type
 *
 * [CAPTURE] is a face detection process by capture the camera.
 * [LIVENESS] is a face detection process by stream the camera.
 * */
enum class LiveFaceXDetectionType {
    CAPTURE, LIVENESS
}